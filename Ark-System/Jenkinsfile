pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                echo 'Code already checked out by SCM'
            }
        }

        stage('Build') {
            steps {
                echo 'CI 构建阶段（占位）'
            }
        }
    }
}
